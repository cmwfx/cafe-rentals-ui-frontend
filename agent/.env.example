# Supabase URL (required)
SUPABASE_URL=https://xlcjopawbjhnlgxvixsl.supabase.co

# Supabase Service Role Key (required)
# Note: This requires a service role key, not the public anon key
SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: Specify a machine ID if you want to use an existing one
# If not provided, the agent will auto-register based on hostname
# MACHINE_ID=uuid-of-existing-machine 